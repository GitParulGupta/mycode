package com.example.practice.code.stackandqueues;

/**
 * s = (p+q)*(m-n)
 * For infix to postfix
 * If s[i] belongs to A-Z or a-z or 0-9 then add to answer
 * If s[i] == ')' the pop till you get '(' and add to all popped elements to answer
 * If s[i] == '(' then push s[i] to stack
 *
 *
 */
public class InfixToPostfix {
}
